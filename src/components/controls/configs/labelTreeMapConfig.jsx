const label = [
  {
    label: "loc",
    value: "loc"
  },
  {
    label: "name",
    value: "name"
  }
  // {
  //   label: "d => `${d.name} (${d.loc})`",
  //   value: "d => `${d.name} (${d.loc})`"
  // }
];

export const labelTreeMapConfig = {
  label
};
