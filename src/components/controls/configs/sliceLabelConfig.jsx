const sliceLabel = [
  {
    label: "id",
    value: "id"
  },
  {
    label: "value",
    value: "value"
  }
  // {
  //   label: "d => `${d.id}:${d.value}`",
  //   value: "d => `${d.id}:${d.value}`"
  // }
];

export const sliceLabelConfig = {
  sliceLabel
};
